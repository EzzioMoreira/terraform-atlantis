projects:
- dir: default
   workflow: default
workflows:
  default:
    plan:
      steps:
      - env:
           name: AWS_PROFILE
           value: default
      - init
      - plan
    apply:
      steps:
      - env:
           name: AWS_PROFILE
           value: default
      - apply